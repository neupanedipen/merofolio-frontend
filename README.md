Front end for MeroFolio, Personal Stock Portfolio Tracker for minor project.

## Run this project as

- `npm install` to install all the required dependencies
- `npm run` to start the project in local host
